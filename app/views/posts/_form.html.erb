<%= form_with(model: @post, local: true) do |form| %>
  <div class="post_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
<%= form.submit %>
<% end %>
<%= link_to "Twitter一覧に戻る", posts_path %>
